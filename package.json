{
  "name": "med-system",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de atendimento clinico completo",
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stohler/med-system.git"
  },
  "license": "MIT"
}
